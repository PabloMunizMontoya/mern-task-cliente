//140 recorderis : aca creamos el context para poder usarlo en el provider de el state, la variable alertasContext toma el valor de una function dada por context.

import { createContext } from "react";

const alertasContext = createContext()

export default alertasContext